httpApi:
  host: 0.0.0.0
  port: 3000
  secret: 'secret'

metrics:
  enabled: true
  host: 0.0.0.0
  port: 9090

logging:
  global: info
  metricsServer: info
  appServer: info
  database: info

database:
  dsn: 'postgres://postgres:pass@localhost:5432/codex-notes'
